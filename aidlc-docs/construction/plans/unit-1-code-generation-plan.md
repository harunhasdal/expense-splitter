# Code Generation Plan — Unit 1: Backend API & Data Layer

## Unit Context

- **Workspace root**: `/Users/hhasdal/code/demos/ai-dlc-projects/aidlc-with-claude`
- **Code location**: `backend/` (monorepo pattern — multi-unit)
- **Stories**: US-1-1 through US-1-8, US-2-7 (partial — immutability enforcement)
- **Dependencies**: None (Unit 1 is first; no upstream units)
- **Outputs consumed by**: Unit 2 (ORM models, repositories), Unit 3 (REST API contract, OpenAPI schema)
- **Project type**: Greenfield multi-unit monorepo

## Story Traceability

- [x] US-1-1 OAuth2 Sign-In
- [x] US-1-2 Create Group
- [x] US-1-3 Add Member
- [x] US-1-4 Remove Member
- [x] US-1-5 Archive Group
- [x] US-1-6 Log Expense
- [x] US-1-7 Record Settlement
- [x] US-1-8 View Group Expenses
- [x] US-2-7 Expense Immutability (HTTP enforcement)

---

## Generation Steps

### Step 1: Project Structure & Configuration
- [x] Create `backend/pyproject.toml` — all pinned dependencies (FastAPI, SQLAlchemy, Alembic, Pydantic, python-jose, httpx, structlog, itsdangerous, uvicorn, gunicorn, aiosqlite, asyncpg, ruff, mypy, pytest, pytest-asyncio, pytest-cov, pip-audit)
- [ ] Create `backend/.python-version` — `3.12`
- [ ] Create `backend/uv.lock` — lock file (generated by `uv sync`)
- [ ] Create `backend/.env.example` — all 14 required env vars with placeholder values
- [ ] Create `backend/Dockerfile` — multi-stage (builder + runtime, python:3.12-slim pinned digest)
- [ ] Create `backend/.dockerignore`
- [ ] Create `backend/ruff.toml` — linting + formatting config
- [ ] Create `backend/mypy.ini` — strict mode config
- [ ] Create `backend/pytest.ini` — asyncio mode, test paths, coverage config

### Step 2: Core Infrastructure
- [ ] Create `backend/core/__init__.py`
- [ ] Create `backend/core/config.py` — Pydantic Settings with all 14 env vars
- [ ] Create `backend/core/db.py` — async SQLAlchemy engine + session factory; `get_db` dependency; `pool_size=2, max_overflow=8, pool_pre_ping=True`
- [ ] Create `backend/core/logging.py` — structlog JSON config; correlation ID binding
- [ ] Create `backend/core/middleware.py` — CorrelationIDMiddleware, SecurityHeadersMiddleware, CSRFMiddleware
- [ ] Create `backend/core/errors.py` — global_exception_handler; typed exception routing
- [ ] Create `backend/main.py` — FastAPI app factory; register all routers + middleware; lifespan context manager

### Step 3: Auth Domain
- [ ] Create `backend/auth/models.py` — SQLAlchemy `User` ORM model
- [ ] Create `backend/auth/schemas.py` — Pydantic response schemas
- [ ] Create `backend/auth/repository.py` — `get_by_id`, `get_by_email`, `upsert_oauth_user`
- [ ] Create `backend/auth/service.py` — `get_authorization_url`, `handle_callback` (WF-1 full flow), `validate_token`, `logout`
- [ ] Create `backend/auth/middleware.py` — `get_current_user` FastAPI dependency; JWT RS256 validation
- [ ] Create `backend/auth/router.py` — `GET /auth/{provider}/login`, `GET /auth/{provider}/callback`, `POST /auth/logout`
- [ ] Stories: US-1-1

### Step 4: Auth Tests
- [ ] Create `backend/tests/__init__.py`
- [ ] Create `backend/tests/conftest.py` — async test client, test DB setup/teardown, override `get_db` + `get_current_user` dependencies, mock OAuth2 provider
- [ ] Create `backend/tests/unit/__init__.py`
- [ ] Create `backend/tests/integration/__init__.py`
- [ ] Create `backend/tests/integration/test_auth.py` — sign-in success, callback invalid state, session expiry, logout

### Step 5: Groups Domain
- [ ] Create `backend/groups/models.py` — SQLAlchemy `Group`, `Member` ORM models
- [ ] Create `backend/groups/schemas.py` — Pydantic request/response schemas
- [ ] Create `backend/groups/repository.py` — all GroupRepository + MemberRepository methods
- [ ] Create `backend/groups/service.py` — all GroupService operations (WF-2 through WF-5)
- [ ] Create `backend/groups/router.py` — all group + member endpoints
- [ ] Stories: US-1-2, US-1-3, US-1-4, US-1-5

### Step 6: Groups Tests
- [ ] Create `backend/tests/integration/test_groups.py` — create group, add/remove member (happy path + guards), archive group (zero-balance, non-zero, force), IDOR tests (non-member gets 404)

### Step 7: Expenses Domain
- [ ] Create `backend/expenses/models.py` — SQLAlchemy `Expense`, `ExpenseSplit` ORM models
- [ ] Create `backend/expenses/schemas.py` — Pydantic schemas incl. split detail types per split mode
- [ ] Create `backend/expenses/repository.py` — all ExpenseRepository methods incl. `get_all_for_balance` projection
- [ ] Create `backend/expenses/service.py` — `create_expense` (WF-6), `list_expenses` (WF-8), `archive_expense` (WF-9 sub-flow)
- [ ] Create `backend/expenses/router.py` — expense endpoints; 405 on PUT/DELETE (WF-9)
- [ ] Stories: US-1-6, US-1-8, US-2-7

### Step 8: Expenses Tests
- [ ] Create `backend/tests/integration/test_expenses.py` — create expense (4 split types), invalid split validation, future date rejection, immutability (405 on PUT/DELETE), archive (owner-only), paginated list, filter by payer

### Step 9: Settlements Domain
- [ ] Create `backend/settlements/models.py` — SQLAlchemy `Settlement` ORM model
- [ ] Create `backend/settlements/schemas.py`
- [ ] Create `backend/settlements/repository.py` — `create`, `list_for_group`, `get_all_for_balance`
- [ ] Create `backend/settlements/service.py` — `create_settlement` (WF-7), `list_settlements`
- [ ] Create `backend/settlements/router.py` — settlement endpoints
- [ ] Stories: US-1-7

### Step 10: Settlements Tests
- [ ] Create `backend/tests/integration/test_settlements.py` — create settlement (happy path, zero-amount, self-settlement), list settlements, non-member blocked

### Step 11: Balance Router (stub for Unit 2 integration)
- [ ] Create `backend/balance/__init__.py`
- [ ] Create `backend/balance/schemas.py` — `MemberBalance`, `SettlementSuggestion`, `CurrencyBalances` Pydantic schemas
- [ ] Create `backend/balance/router.py` — `GET /groups/{id}/balances`, `GET /groups/{id}/settlements/suggestions` — stubs returning 501 until Unit 2 engine is wired
- [ ] Note: `balance/engine.py` and `balance/service.py` generated in Unit 2

### Step 12: Database Migrations
- [ ] Run `alembic init backend/migrations` — create Alembic environment
- [ ] Create `backend/migrations/env.py` — configure async SQLAlchemy engine; import all models for autogenerate
- [ ] Create `backend/migrations/alembic.ini` — point to `DATABASE_URL` env var
- [ ] Generate initial migration: `alembic revision --autogenerate -m "initial_schema"` — creates all 5 tables (users, groups, members, expenses, expense_splits, settlements)
- [ ] Review and adjust generated migration for custom constraints (unique indexes, check constraints)

### Step 13: Deployment Artifacts
- [ ] Create `docker-compose.yml` at repo root — `db` (postgres:16-alpine), `api` (backend/), `frontend` placeholder
- [ ] Create `.env.example` at repo root — all 14 env vars
- [ ] Create `.github/workflows/ci.yml` — backend-ci job (ruff, mypy, pytest, pip-audit)
- [ ] Create `.github/workflows/deploy.yml` — build-push, deploy-staging (migration task + service update), deploy-prod gate, deploy-frontend
- [ ] Create `cdk/` directory structure — `bin/expense-splitter.ts`, `lib/network-stack.ts`, `lib/application-stack.ts`, `lib/frontend-stack.ts`, `cdk.json`, `package.json`

### Step 14: Code Documentation Summary
- [ ] Create `aidlc-docs/construction/unit-1-backend-api/code/code-summary.md` — list of all created files with descriptions

---

## Security Compliance Checklist (pre-completion gate)

Before marking Code Generation complete, verify:
- [x] SECURITY-01: DATABASE_URL via Secrets Manager; TLS enforced in RDS config
- [ ] SECURITY-03: structlog JSON configured; no PII in log fields; correlation ID present
- [ ] SECURITY-04: SecurityHeadersMiddleware applied to all responses
- [ ] SECURITY-05: All Pydantic schemas have max_length; no raw SQL; EmailStr used
- [ ] SECURITY-08: `get_current_user` dep on all protected routes; CORS restricted; CSRF middleware; object-level checks in all services
- [ ] SECURITY-09: global_exception_handler returns generic messages; `/docs`/`/redoc` disabled in prod
- [ ] SECURITY-10: `uv.lock` committed; pip-audit in CI; Dockerfile pinned base image
- [ ] SECURITY-11: CSRF middleware; auth logic isolated in `auth/`
- [ ] SECURITY-12: HttpOnly+Secure+SameSite cookie; RS256 JWT; no password storage
- [ ] SECURITY-13: 405 on expense/settlement mutation; audit log on archive events
- [ ] SECURITY-15: global_exception_handler at app entry; all DB calls in try/except
